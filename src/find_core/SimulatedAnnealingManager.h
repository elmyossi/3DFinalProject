//
// Created by Yossi Elman on 2018-12-22.
//

#ifndef INC_3DFINALPROJECRT_SIMULATEDANNEALING_H
#define INC_3DFINALPROJECRT_SIMULATEDANNEALING_H
/*
#include "../utils/Utils.h"

class SimulatedAnnealingManager {

public:
    // Initial and final temperature
    static double dist;

    // Simulated Annealing parameters

    // Temperature at which iteration terminates
    static const double distMin;

    // Decrease in temperature
    static const double alpha;

    // Number of iterations of annealing
    // before decreasing temperature
    static const int numIterations;

    // Locational parameters

    // Target array is discretized as M*N grid
    static const int M, N;

    // Number of objects desired
    static const int k;
private:
    double cost(int[] inputConfiguration);
    static double getAreaOfTriangle(RowVector3 v1, RowVector3 v2, RowVector3 v3);
    static RowVector3 Centroid(RowVector3 p1, RowVector3 p2, RowVector3 p3, RowVector3 p4);
    static double SignedVolume(RowVector3 p1, RowVector3 p2, RowVector3 p3, RowVector3 p4);



};

*/
#endif //INC_3DFINALPROJECRT_SIMULATEDANNEALING_H
